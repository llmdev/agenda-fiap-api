{
    "version": 2,
    "installCommand": "npm run install",
    "buildCommand": "npm run build",
    "builds": [
        {
            "src": "*.js",
            "use": "@vercel/node"
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "/"
        }
    ]
}